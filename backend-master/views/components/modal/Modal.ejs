<style>
    .general-modal {
        display: none;
        position: fixed;
        z-index: 99;
        /* Sit on top */
        left: 0;
        top: 0;
        width: 100%;
        /* Full width */
        height: 100%;
        /* Full height */
        overflow: auto;
        /* Enable scroll if needed */
        background-color: rgb(0, 0, 0);
        /* Fallback color */
        background-color: rgba(0, 0, 0, 0.4);
        /* Black w/ opacity */
    }
    
    .general-modal-content {
        background-color: #fefefe;
        margin: auto;
        padding: 20px;
        border: 1px solid #888;
        min-width: 400px;
    }
    
    .closeGeneralModal {
        color: #aaaaaa;
        float: right;
        font-size: 28px;
        font-weight: bold;
    }
    
    .closeGeneralModal:hover,
    .closeGeneralModal:focus {
        color: #000;
        text-decoration: none;
        cursor: pointer;
    }
</style>

<!-- The Modal -->
<div id="myGeneralModal" class="general-modal">

    <!-- Modal content -->
    <div class="general-modal-content">
        <span class="closeGeneralModal">&times;</span>
        <div id="myGeneralModalContent">Some text in the Modal..</div>
    </div>

</div>

<script>
    // Get the modal
    var modal = document.getElementById("myGeneralModal");
    var modalContent = document.getElementById("myGeneralModalContent");
    // Get the <span> element that closeGeneralModals the modal
    var span = document.getElementsByClassName("closeGeneralModal")[0];

    function modal_show(content) {
        modalContent.innerHTML = content;
        modal.style.display = "flex";
    }

    // When the user clicks on <span> (x), closeGeneralModal the modal
    span.onclick = function() {
        modal.style.display = "none";
    }

    // When the user clicks anywhere outside of the modal, closeGeneralModal it
    window.onclick = function(event) {
        if (event.target == modal) {
            modal.style.display = "none";
        }
    }
</script>